<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Color del mockup -->
    <solid android:color="#E3EED7"/>

    <!-- Bordes redondos -->
    <corners android:radius="16dp"/>

    <!-- Sombra sutil opcional -->
    <padding
        android:left="6dp"
        android:top="6dp"
        android:right="6dp"
        android:bottom="6dp"/>
</shape>